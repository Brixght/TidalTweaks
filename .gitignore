__pycache__/
*.pyc
dist/
build/
*.spec.bak
.venv/
venv/

node_modules/

# NEVER commit license codes or the minting key
*-codes.txt
PRIVATE-KEY*
*.pem
# Seller ops notes — private, local only (never on GitHub)
SELLER.md
